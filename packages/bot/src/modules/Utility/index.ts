import Module from '../../core/structures/Module';

export default class Utility extends Module {
        public info = 'Commands to provide information about Discord objects';
        public aliases = ['util', 'utils'];
        public allowedByDefault = true;
}
